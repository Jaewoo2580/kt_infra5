# 리눅스 스터디 과제모음
Linux Study

# 스터디 강의 스크립트 파일 전체 모음 입니다.

# 1. 루트 디렉토리에 example 폴더 생성 및 이동
mkdir -p ~/example && cd ~/example

# 2. 파일 다운로드 및 압축 해제
curl -L "https://github.com" -o example.zip && unzip example.zip && rm example.zip

## 🚀 실습 파일 다운로드 및 설정

1. **루트 디렉토리에 `example` 폴더 생성 및 이동**
2. **실습 파일 다운로드 (`?raw=true` 포함)**
3. **압축 해제 후 원본 zip 파일 삭제**

```bash
mkdir -p ~/example && cd ~/example && curl -L "https://github.com" -o example.zip && unzip example.zip && rm example.zip
```
